# =============================================================================
# DistribAI — Worker-only requirements (contributors, Colab, Kaggle, VPS)
# =============================================================================
#
#   pip install -r requirements-worker.txt
#
# Does NOT install orchestrator-only deps (redis, aiosqlite admin stack, etc.).
# Clone the public grid repo (worker/ tree) or full monorepo before running:
#   python -m worker.src.daemon.run
#
# CPU-only PyTorch by default. For NVIDIA CUDA: requirements-cuda.txt instead.
# =============================================================================

# --- ML & Compute ---
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0
numpy>=1.24.0

# --- gRPC ---
grpcio==1.80.0
grpcio-tools==1.80.0
protobuf>=6.31.1,<7.0.0

# --- HTTP (registration) ---
aiohttp>=3.10.11

# --- AWS / Storage (optional S3 gradient paths) ---
boto3>=1.34.0
aioboto3>=2.9.0

# --- Security ---
pyjwt>=2.8.0
cryptography>=46.0.7

# --- Configuration ---
python-dotenv>=1.0.0

# --- System ---
psutil>=5.9.0

# --- Validation (worker payload schemas) ---
pydantic>=2.0.0
